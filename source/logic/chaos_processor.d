module logic.chaos_processor;

import std.random;

import random_stuff;

import types.graphics;
import types.world;
import types.game_elements;

public void chaosProcessor(WorldState world, float dt) {
  // auto entities = world.entities;

  // float threshold = 0.1;
  // if (uniform(0.0, 1.0, rng) < threshold) {
  //   auto newMomentum = [uniform(-3, 3, rng), uniform(-3, 3, rng)];
  //   entities[uniform(0, entities.length, rng)]._momentum = newMomentum;
  // }
}
